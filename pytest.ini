[pytest]
addopts = --tb=short
testpaths = tests app/tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    integration: marks tests as integration tests
    production: marks tests as production environment validation tests
